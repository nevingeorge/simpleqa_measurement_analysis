# Local-side requirements (analysis + orchestration).
# Modal containers install their own deps inside the image definitions
# in modal_eval/app.py — you do not need vllm, torch, or transformers locally.
#
# To recreate the environment:
#   python3 -m venv sqa_eval
#   source sqa_eval/bin/activate
#   pip install -r requirements.txt

modal==1.4.3
pandas==3.0.3
pyarrow==24.0.0
numpy==2.4.6
scipy==1.17.1
statsmodels==0.14.6
matplotlib==3.10.9
requests==2.34.2
